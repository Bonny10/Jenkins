pipeline{
	agent any

	stages{
		stage("Build"){
			steps{
				echo "We are in build task"
			}
		}
		stage("test"){
			steps{
				echo "We are in test task.."
			}
		}
		stage("deploy"){
			steps{
				echo "We are in deploy task"
			}
		}
	}
}

